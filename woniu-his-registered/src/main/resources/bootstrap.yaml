spring:
  cloud:
    nacos:
      discovery: #注册中心配置
        username: nacos
        password: nacos
        server-addr: http://192.172.0.12:8848
        namespace: public
        group: group1
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml